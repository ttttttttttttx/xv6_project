当两个线程同时向哈希表中添加条目时 (ph 2)
它们的总插入速率为每秒 46857 次
大约是运行 ph 1 的单线程速度的两倍

观察 ph.c 中与插入相关的源码 insert 函数
这段代码是多线程操作哈希表时出错的根源
假设某个线程调用了 insert() 但没有返回
此时另一个线程调用 insert()
就可能发生漏插入键值对的现象
insert 操作不是原子的

因为存在竞争条件，两个线程竞争共享内存映射